/*
import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PrototypeRemoteViewPage } from './prototype-remote-view.page';
import { CompileModule } from 'p3x-angular-compile';
import { mockImports } from '../../shared/ionic-fake/mock.imports';
import { mockProviders } from '../../shared/ionic-fake/mock.providers';

describe('PrototypeRemoteViewPage', () => {
  let component: PrototypeRemoteViewPage;
  let fixture: ComponentFixture<PrototypeRemoteViewPage>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      imports: mockImports.concat([
           CompileModule
      ]),
      providers: mockProviders.concat([

      ]),
      declarations: [ PrototypeRemoteViewPage ],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PrototypeRemoteViewPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

*/
