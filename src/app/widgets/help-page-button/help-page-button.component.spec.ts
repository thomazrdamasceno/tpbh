
/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { HelpPageButtonComponent } from './help-page-button.component';
import { mockImports } from '../../shared/ionic-fake/mock.imports';
import { mockProviders } from '../../shared/ionic-fake/mock.providers';

describe('HelpPageButtonComponent', () => {
  let component: HelpPageButtonComponent;
  let fixture: ComponentFixture<HelpPageButtonComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      imports: mockImports.concat([

      ]),
      providers: mockProviders.concat([

      ]),
      declarations: [ HelpPageButtonComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(HelpPageButtonComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

*/
